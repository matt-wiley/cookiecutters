from setuptools import setup, find_packages

setup(
    name='{{cookiecutter.app_name}}',
    version='0.0.1',
    url='',
    author='',
    author_email='',
    description='',
    packages=find_packages(),    
    install_requires=[],
)
